services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: gymflow
      POSTGRES_USER: gymflow_user
      POSTGRES_PASSWORD: gymflow123
    ports:
      - "5432:5432"
    volumes:
      - gymflow_data:/var/lib/postgresql/data

volumes:
  gymflow_data: